name: first-workflow
on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo "hello world"
      - name: echo multiple commands
        run: node -v
